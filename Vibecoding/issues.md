In two way sync:
- when a folder is deleted in one location, it should be deleted in the other location.

Currently, deleting a folder on google drive, does not delete the folder on the local machine. Instead it is recreated on google drive.

Seems to be working for files, but not for folders.

Files added to folder:
I created a folder locally, it gets synced to google drive.
I added 4 files to the folder locally, but they do not get synced.