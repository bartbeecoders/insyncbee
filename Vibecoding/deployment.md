Create a full github pipeline that:
- builds and deploys the insyncbee-portal to the k3s cluster
- builds the insyncbee-db-service for windows, linux and mac
- make sure we have a download page for the db service

set the tag to 0.1.0


Update the version nr (0.2.0)
Include the changelog in the release. (about page)
Update the portal with all new features and fixes.
Check the build for warnings or error and correct them.
commit, tag and push to github



How difficult is it to run this app as a background service (on endevour os linux) with a tray icon?