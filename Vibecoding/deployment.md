Create a full github pipeline that:
- builds and deploys the insyncbee-portal to the k3s cluster
- builds the insyncbee-db-service for windows, linux and mac
- make sure we have a download page for the db service

set the tag to 0.1.0
